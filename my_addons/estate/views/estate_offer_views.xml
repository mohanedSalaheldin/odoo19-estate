<?xml version='1.0' encoding='utf-8'?>
<odoo>
    <!-- view -->
    <record id="estate_offer_view_list" model="ir.ui.view">
        <field name="name">estate.offer.view.list</field>
        <field name="model">estate.offer</field>
        <field name="arch" type="xml">
            <list string="Property Offer">
                <field name="price"/>
                <field name="property_id"/>
                <field name="partner_id"/>
                <field name="validity"/>
                <field name="date_deadline"/>
                <button name="action_accept" type="object" icon="fa-check" string="Accept"/>
                <button name="action_refuse" type="object" icon="fa-times" string="Refuse"/>
                <field name="status"/>
            </list>
        </field>
    </record>
    <!-- form -->
    <record id="estate_offer_view_form" model="ir.ui.view">
        <field name="name">estate.offer.view.form</field>
        <field name="model">estate.offer</field>
        <field name="arch" type="xml">
            <form string="Property Offer">
                <sheet>
                    <group>
                        <field name="price"/>
                        <field name="property_id"/>
                        <field name="partner_id"/>
                        <field name="validity"/>
                        <field name="date_deadline"/>
                        <field name="status"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>
    <!-- action and the menuitem -->
    <record id="real_estate_deals_action" model="ir.actions.act_window">
        <field name="name">Offers</field>
        <field name="res_model">estate.offer</field>
        <field name="view_mode">list,form</field>
    </record>
</odoo>